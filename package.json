{
  "name": "samd-audit-trail",
  "version": "1.0.0",
  "description": "Blockchain-based AI-SaMD Change Management Audit Trail System (DMPA)",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
